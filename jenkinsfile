pipeline {
    agent { node { label 'raspberry pi' } } 
    stages {
        stage('test') {
            steps {
                sh " cd /home/pi/Desktop"  
                sh "pwd"
                sh "python3 test.py "
            }
        }
    }
}
